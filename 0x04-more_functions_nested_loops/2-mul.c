#include "main.h"
/**
* mul - function that multiplies two integers
* @a: takes in an integer
* @b: takes in an integer
* Return: the result from multiplying two integers
**/
int mul(int a, int b)
{
	return (a * b);
}
